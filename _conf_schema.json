{
  "cookie": {
    "description": "Cookie JSON（{name: value} 格式）",
    "type": "text",
    "default": "",
    "hint": "浏览器 F12 → Application → Cookies → 智慧树站点 → 导出为 JSON 粘贴到这里。保存后重启插件或执行 /zhihuishu cookie 生效。"
  }
}
